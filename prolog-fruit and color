% fruit(Fruit, Color).
fruit(apple, red).
fruit(banana, yellow).
fruit(grape, purple).
fruit(orange, orange).

% Query will backtrack to find all possibilities
Example Query:

?- fruit(F, C).
F = apple,
C = red ;
F = banana,
C = yellow ;
F = grape,
C = purple ;
F = orange,
C = orange
